[pytest]
# Isolated pytest config for Skyramp-generated tests.
# Prevents pytest from traversing up to the repo root pyproject.toml,
# which references sqlalchemy filterwarnings not available in the Skyramp runner.
